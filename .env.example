# Local dev — these are the defaults, no need to set unless ports differ
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000

# Production — set these in Vercel / Netlify dashboard
# VITE_API_URL=https://your-vaultx-api.onrender.com
# VITE_WS_URL=wss://your-vaultx-api.onrender.com
